/**
 * Tipos TypeScript para feature de formulário de clientes.
 * 
 * Define interfaces para dados de clientes, formulários
 * e ViewModels conforme Blueprint Arquitetural.
 */

// Placeholder para tipos da feature customer-form
// A implementação completa será realizada nas próximas fases do projeto

export interface CustomerListViewModel {
  id: number;
  name: string;
  documentNumberFormatted: string;
  phone: string;
  city: string;
  activeLoansCount: number;
}