apiVersion: v1
kind: ConfigMap
metadata:
  name: seed-data-release-policy
  labels:
    app.kubernetes.io/name: seed-data
    app.kubernetes.io/part-of: seed-data
data:
  off_peak_window_utc: "02:00-05:00"
  rollback_enabled: "true"
  seed_data_app: "seed-data-pipeline"
  argocd_server: "https://argocd-server.argocd.svc.cluster.local"
